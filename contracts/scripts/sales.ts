import {ethers} from 'hardhat';

/**
 * This file will contain the logic to let you in just one command grab all the configuration
 * and deploy all the sales needed on the right network !
 */

async function main() {}

main()
  .then(() => process.exit(0))
  .catch((error) => {
    console.error(error);
    process.exit(1);
  });
